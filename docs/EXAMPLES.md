# Examples

Available domain templates:

- `saas-support`
- `travel-concierge`
- `research-ops`

These templates ship in the packaged runtime under `kit/domain-templates/`.

Initialize one with:

```bash
node ./bin/vertical-agent-forge.mjs init --domain saas-support
```

One-click profiles are listed under `examples/profiles/`.
